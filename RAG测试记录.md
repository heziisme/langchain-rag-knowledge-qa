# RAG知识库问答系统 测试记录
## 测试环境
- Python + LangChain + Chroma + DeepSeek‑Chat
- 知识库文档：docs/sample_game_info.md（二次元手游行业知识库）

## 测试用例
|测试问题|预期结果|实际结果|备注|
|---|---|---|---|
|二次元手游玩家主要有哪些负面反馈？|返回角色剧情、付费抽卡、玩法、运营社区四类负面反馈|正常返回全部要点|修复距离判断逻辑后通过|
|二次元手游有哪些主流玩法？|返回卡牌回合、ARPG、塔防、大世界、恋爱养成玩法|正常返回全部玩法|修复距离判断逻辑后通过|
|二游的商业模式是什么？|返回抽卡为主、大保底、专武拆分、降氪趋势、多端发行|正常输出付费商业模式内容|修复距离判断逻辑后通过|
|二次元手游行业未来有什么发展趋势？|返回差异化内容、外观付费、AI生产、IP跨媒介、多端互通|正常输出趋势要点|修复距离判断逻辑后通过|
|二游长线运营需要注意哪些点？|版本节奏、福利投放、角色平衡、社区响应、IP衍生拓展|完整输出五点运营策略|修复距离判断逻辑后通过|

## 开发遇到的问题与解决方案
### Bug1：检索拿到片段，但是全部输出「知识库中没有找到相关信息」
- 现象：向量检索成功返回多条知识库片段，但是LLM始终回答无相关信息
- 根因：Chroma的`similarity_search_with_score`返回的是**欧氏距离，数值越小代表相似度越高**；代码把大小判断条件写反，全部有效片段被过滤，传入LLM的上下文为空。
- 解决：修正距离判断逻辑；调整阈值，距离小于阈值才保留片段。

### Bug2：修改知识库文档后，检索依旧返回旧内容
- 根因：旧向量数据库chroma_db文件夹没有删除，程序加载历史旧向量库。
- 解决：新增/修改知识库文档，必须删除chroma_db文件夹，重新运行程序构建向量库。

## 项目优化点说明
1. 调整chunk_size、chunk_overlap，保证文本语义完整性；
2. Top‑K多召回，基于欧氏距离做片段过滤，筛除低相关性片段；
3. Prompt约束大模型，知识库无资料禁止编造，抑制幻觉；
4. 控制台打印检索片段+距离分数，便于调试检索效果。
